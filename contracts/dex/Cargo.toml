[package]
name = "dex-instance"
version = "0.1.0"
edition = "2021"

[dependencies]
ckb-std = "1.1.0"

[build-dependencies]
cc = "1.2.58"

[profile.release]
overflow-checks = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
